/** BullMQ queue name — must match Worker subscription. */
export const INTERVIEW_QUEUE = "interview-queue";

/** Job `name` passed to `Queue#add` for evaluate-answer processing. */
export const EVALUATE_ANSWER = "evaluate-answer";
